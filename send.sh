# prompt user for path of folder
#		without trailing space or no?

# make a variable to represent path of folder on local system

# prompt user for name of s3 bucket

# make a variable to represent the name of the s3 bucket

# mhl seal the source folder with xxHash
#		what's the best practice for a binary that requires to be in the same directory?

# mb

# sync into the bucket

# passthrough the info that the aws s3 cli would be displaying?

# let user know that the sealed upload to s3 has completed
